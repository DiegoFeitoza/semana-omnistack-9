import React from 'react';
import Routes from './src/routes';

//View == <div> box de container
//Text == <qualquer tag de texto> Qualquer campo de texto, e tudo tem que ser estilizado individualmente
//StyleSheet == 
//Para aplicar css no Native, é preciso criar um style dentro da tag que vai usar a estilização
//O react Native utiliza flex-box *importante


export default function App() {
  return (<Routes />);
}
